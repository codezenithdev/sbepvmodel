"""Solar Agent: the OpenAI-backed chat assistant embedded in the dashboard.

Split by what each part answers to:

- ``prompts``      -- system instructions and static model notes (prose)
- ``tool_schemas`` -- the tool contract the model sees (pure data)
"""
